function healthyShopping(array){
    let result = [];

    for(let element of array){
        let object = {
            name: element,
            status:"filter"
        };
    };
    result.push(object);
};
    module.exports = (healthyShopping);
